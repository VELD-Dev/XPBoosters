package com.velddev.xpalchemy;

public class CommonMain {

    public static void init() {
    }

    public static float roundToHalf(float a) {
        return Math.round(a * 2) / 2.0f;
    }

    public static double roundToHalf(double a) {
        return Math.round(a * 2) / 2.0f;
    }
}